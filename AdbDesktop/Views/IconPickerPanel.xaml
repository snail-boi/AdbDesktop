<UserControl x:Class="AdbDesktop.IconPickerPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:AdbDesktop">

    <DockPanel Margin="20">

        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,14">
            <TextBlock Text="{Binding Heading}" FontSize="16"/>
            <TextBlock Text="{Binding SubHeading}" FontSize="12"
                       Foreground="{StaticResource MutedTextBrush}" Margin="0,5,0,0"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <Grid DockPanel.Dock="Bottom" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Border Width="40" Height="40" CornerRadius="9" Background="#1E1E24" Margin="0,0,10,0">
                    <Image Source="{Binding SelectedImage}" Width="34" Height="34"
                           RenderOptions.BitmapScalingMode="HighQuality"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Selected" FontSize="11" Foreground="{StaticResource MutedTextBrush}"/>
                    <TextBlock Text="{Binding SelectedLabel}" FontSize="12.5"
                               MaxWidth="300" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Cancel" Style="{StaticResource SecondaryButtonStyle}"
                        Margin="0,0,8,0" Command="{Binding CancelCommand}"/>
                <Button Content="Use this icon" Style="{StaticResource PrimaryButtonStyle}"
                        IsEnabled="{Binding HasSelection}" Command="{Binding ConfirmCommand}"/>
            </StackPanel>
        </Grid>

        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <ItemsControl x:Name="Tiles">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>
